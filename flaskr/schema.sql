drop table if exists entries;
create table entries (
  name     VARCHAR(100),
  lastname     VARCHAR(100),  
  zipcode     VARCHAR(5),
  email    VARCHAR(100),
  username VARCHAR(30), 
  password VARCHAR(100)
);